# Standalone install for the Phase 2 scripts. From the repo root you can instead run
#   pip install -e ".[processing]"
# torch and scipy are not imported directly but are required at runtime:
# transformers needs a backend, and networkx.pagerank needs scipy.
pymongo>=4.18,<5
python-dotenv>=1.2,<2
spacy>=3.7,<3.8
networkx>=3.6,<4
scipy>=1.17,<2
transformers>=5.16,<6
torch>=2.14,<3

# spaCy model, installed separately:
#   python -m spacy download en_core_web_sm
